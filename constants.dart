import 'package:flutter/material.dart';

enum menuState { home, balance, subscriptions, search }

const activeButtonColor = Color.fromARGB(255, 9, 69, 165);
const inactiveButtonColor = Color.fromARGB(255, 166, 170, 174);
